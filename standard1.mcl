macro INEVITABLE (A) =
    mu X . (< true > true and [ not (A) ] X)
end_macro

macro FAIR (A) =
    [ (not (A))* ] < (not (A))* . (A) > true
end_macro

macro CYCLE (R) =
    nu X . < (R) > X
end_macro